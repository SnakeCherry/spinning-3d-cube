#include "Cube.hpp"

Cube::Cube(float cubeWidth, float incrementSpeed):cubeWidth_(cubeWidth), incrementSpeed_(incrementSpeed){}

void Cube::calculateForSurface_(Vec3 &cubePoint, int ch) const {
    
    

} 
